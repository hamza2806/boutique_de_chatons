<%= render 'showitem'%>

<% if @item.admin == current_user %>
		<h5>Ajouter une photo à votre article</h5>
		    <%= form_tag item_item_pictures_path(@item), multipart: true, method: :post, remote: true do %>
		    <%= file_field_tag :item_pictures %>
		    <%= submit_tag "Add" %>
		    <% end %>
<%end%>